/*
Word Counter
Create a struct WordCounter that tracks the number of times each word appears in a given text.

Input Example:

rust
Copy code
let mut counter = WordCounter::new();
counter.process_text("hello world hello rust");
println!("{}", counter.count("hello")); // 2
println!("{}", counter.count("world")); // 1
*/

struct WordCounter {
    process_text: String,
}

impl WordCounter {
    fn count(text: String) {
        self.process_text.contains
    }
}

fn main() {
    let mut counter = WordCounter;

    counter.process_text("");
    println!("Hello, world!");
}
